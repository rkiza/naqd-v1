
OPENROUTER_API_KEY=

# Primary model (OpenRouter model ID). Falls back to OPENROUTER_FALLBACK_MODEL on failure.
OPENROUTER_MODEL=google/gemini-3.5-flash

# GPT fallback when the primary model fails.
OPENROUTER_FALLBACK_MODEL=openai/gpt-4o-mini

