# Anthropic API key for the AI Assistant (server-side only).
# Without it, the assistant gracefully falls back to curated bilingual responses.
ANTHROPIC_API_KEY=

# Model used by the AI Assistant route handler.
ANTHROPIC_MODEL=claude-opus-4-8
